import React, { useState } from 'react';

// The new App component that manages the "pages" with a state variable
const App = () => {
  // Use state to keep track of the current page being displayed
  const [currentPage, setCurrentPage] = useState('quick-actions');

  // Placeholder components for the different "pages"
  const CreateQuizPage = () => (
    <div className="flex items-center justify-center h-screen bg-gray-100 p-4">
      <div className="text-center">
        <h1 className="text-3xl font-semibold text-gray-700">Create Quiz Page</h1>
        <button
          onClick={() => setCurrentPage('quick-actions')}
          className="mt-6 px-6 py-3 bg-gray-500 text-white font-semibold rounded-lg shadow-md hover:bg-gray-600 transition-colors duration-200 ease-in-out"
        >
          Back to Quick Actions
        </button>
      </div>
    </div>
  );

  const TraineesPage = () => (
    <div className="flex items-center justify-center h-screen bg-gray-100 p-4">
      <div className="text-center">
        <h1 className="text-3xl font-semibold text-gray-700">Trainees Page</h1>
        <button
          onClick={() => setCurrentPage('quick-actions')}
          className="mt-6 px-6 py-3 bg-gray-500 text-white font-semibold rounded-lg shadow-md hover:bg-gray-600 transition-colors duration-200 ease-in-out"
        >
          Back to Quick Actions
        </button>
      </div>
    </div>
  );

  const FeedbackPage = () => (
    <div className="flex items-center justify-center h-screen bg-gray-100 p-4">
      <div className="text-center">
        <h1 className="text-3xl font-semibold text-gray-700">Feedback Page</h1>
        <button
          onClick={() => setCurrentPage('quick-actions')}
          className="mt-6 px-6 py-3 bg-gray-500 text-white font-semibold rounded-lg shadow-md hover:bg-gray-600 transition-colors duration-200 ease-in-out"
        >
          Back to Quick Actions
        </button>
      </div>
    </div>
  );

  // The core AdminQuickActions component, now integrated into the App component
  const AdminQuickActions = () => (
    <div className="quick-actions p-6 bg-white rounded-xl shadow-lg flex flex-col items-center space-y-4">
      <h4 className="text-2xl font-bold text-gray-800">Quick Actions</h4>
      <div className="flex flex-col sm:flex-row sm:space-x-4 space-y-4 sm:space-y-0 w-full">
        <button
          onClick={() => setCurrentPage('create-quiz')}
          className="flex-1 px-6 py-3 bg-blue-500 text-white font-semibold rounded-lg shadow-md hover:bg-blue-600 transition-colors duration-200 ease-in-out transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-blue-400 focus:ring-opacity-75"
        >
          <span className="mr-2">➕</span> Create Quiz
        </button>
        <button
          onClick={() => setCurrentPage('trainees')}
          className="flex-1 px-6 py-3 bg-green-500 text-white font-semibold rounded-lg shadow-md hover:bg-green-600 transition-colors duration-200 ease-in-out transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-green-400 focus:ring-opacity-75"
        >
          <span className="mr-2">📋</span> View Trainees
        </button>
        <button
          onClick={() => setCurrentPage('feedback')}
          className="flex-1 px-6 py-3 bg-purple-500 text-white font-semibold rounded-lg shadow-md hover:bg-purple-600 transition-colors duration-200 ease-in-out transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-purple-400 focus:ring-opacity-75"
        >
          <span className="mr-2">💬</span> Review Feedback
        </button>
      </div>
    </div>
  );

  // Conditionally render the correct component based on the current page state
  return (
    <div className="min-h-screen bg-gray-50 flex items-center justify-center p-4">
      {currentPage === 'quick-actions' && <AdminQuickActions />}
      {currentPage === 'create-quiz' && <CreateQuizPage />}
      {currentPage === 'trainees' && <TraineesPage />}
      {currentPage === 'feedback' && <FeedbackPage />}
    </div>
  );
};

export default App;
